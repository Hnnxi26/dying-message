'use client';
import {useRoom} from '@/lib/localStore';
function Chip({word,type}:{word:string;type:'adjective'|'noun'}){return <span className={`inline-flex rounded-xl px-3 py-2 text-sm font-black text-white ${type==='adjective'?'bg-red-600':'bg-blue-600'}`}>{word}</span>}
export default function Projector(){const room=useRoom();if(!room)return <main className="min-h-screen flex items-center justify-center"><h1 className="text-7xl font-black">DYING MESSAGE</h1></main>;const teams=Object.values(room.teams);const done=teams.filter(t=>['complete','success','gameover'].includes(t.status)).length;return <main className="min-h-screen flex items-center justify-center p-10 text-center"><section className="max-w-5xl w-full"><h1 className="text-7xl font-black">DYING MESSAGE</h1><p className="mt-4 text-4xl">ROUND {room.round}</p><p className="mt-4 text-2xl text-white/70">진행 {done} / {teams.length}</p><div className="mt-10 grid grid-cols-3 gap-4 text-left">{(['criminal','weapon','motive'] as const).map(k=><div key={k} className="rounded-3xl bg-white/10 p-6"><h2 className="text-3xl font-black">{k==='criminal'?'범인':k==='weapon'?'도구':'동기'}</h2><div className="mt-4 flex flex-wrap gap-2">{room.hints[k].map(t=><Chip key={t.id} word={t.word} type={t.type}/>)}</div></div>)}</div></section></main>}
